# Secrets de TEST pour ce scénario Molecule uniquement — jamais de vraies valeurs, sur une
# machine jetable qui n'appelle jamais l'API Scalingo pour de vrai (la cron FC reste
# désactivée par défaut, pass_sport_fc_cron_enabled n'est pas mis à true ici).
#
# Noms d'opérateurs inventés par syllabes, sans rapport avec de vraies personnes — même
# principe que les autres fixtures de test du dépôt (voir data/utils/test_codes_utils.py).
scalingo_app: molecule-test-app
scalingo_api_token: tok_molecule_fake_00000000
pass_sport_operators:
  - kelvao
  - brindu
